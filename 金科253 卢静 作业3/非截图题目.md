1. R语⾔最最基本的数据结构是什么？ 向量
可以⽤哪个函数构建它？ c（）
总共有哪些类型？ 字符型:c("","") 数值型c(1.2.3) 逻辑型c(true,false) 因子型factor("a","b")
整数型c(1L,2L) 日期型as.Date(c("2025-11-21")) 列表型
⽤什么函数可以查看该数据结构的类型？ typeof
如何按照序号提取当中的元素？ >x[序号]
2. R语⾔中哪种数据结构可以包含任何其他类型的数据结构？ 列表
可以⽤哪个函数构建它？list()。
如何进⾏当中元素的提取？ 1.数据框名$数据列值   2.数据框名[行，列]
有⼏种⽅式? 2
3. 如果需要进⾏常规的数据统计分析，需要⽤到哪种数据结构？数据框
该种数据结构与前两种数据结构有什么联系？ 数据框从列表与向量中查询数据  >x=data.frame()
4. 上述的三种数据结构有什么关系？ 数据框的每一列都是一个向量，列表可以包含数据框与向量
z<-list()
name<-c("1","2","3")
age<-c("4","5","6")
information<-data.frame(name,age)